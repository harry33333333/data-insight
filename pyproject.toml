[project]
name = "data-insight"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.12"
dependencies = [
    "crewai>=1.15.1",
    "crewai-files>=1.15.1",
    "crewai-tools>=1.15.1",
]

[[tool.uv.index]]
url = "https://mirrors.aliyun.com/pypi/simple"
default = true
